# HR web app

## Setup

### Environment

Copy `.env.example` to `.env` and set proper variables.

### Migrate & seed db

`php artisan migrate`

`php artisan db:seed`

### Run the application

`php artisan serve`

